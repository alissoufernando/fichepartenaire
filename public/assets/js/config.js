var success = localStorage.getItem("success") || '#2a8c28';
var secondary = localStorage.getItem("secondary") || '#1ea6ec';

window.endlessAdminConfig = {
	// Theme success Color
	success: success,
	// theme secondary color
	secondary: secondary,
};
